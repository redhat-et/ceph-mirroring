apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: primary 
  namespace: rook-ceph
  labels:
    name: west2
  annotations:
    apps.open-cluster-management.io/github-path: rbd-jobs/both
    apps.open-cluster-management.io/github-branch: master
spec:
  channel: rook-ceph/wordpress-storage-repo
  name: primary
  placement:
    placementRef:
      kind: PlacementRule
      name: primary 
